// Buffer
const buf1 = Buffer.alloc(10, 1)
const buf2 = Buffer.from([1, 2, 3])
const buf6 = Buffer.from("tést")
const buf7 = Buffer.from("tést", "latin1")

// 编码方式 'utf8', 'utf16le'<'ucs2'>,'latin1', 'base64', 'hex', 'ascii', 'binary'
